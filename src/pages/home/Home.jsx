import { Helmet } from "react-helmet-async";

const Home = () => {
    return (
        <div>
            <Helmet>
                <title>Home || Ship Ninja</title>
            </Helmet>
            home
        </div>
    );
};

export default Home;